---
phase: 03-openvsp-native-integration
task: 2
total_tasks: 2
status: blocked
last_updated: 2026-03-11T23:35:11.063Z
---

<current_state>
Plan 03-05 (gap closure: VSPCheckIsInit bug fix) Task 1 is COMPLETE and committed. Task 2 (human-verify native VSPAERO end-to-end) revealed that while the import fix works perfectly (`has_vsp=True`, native path triggers), the VSPAERO VLM solver pipeline itself has deeper configuration issues that prevent it from producing real aerodynamic results.

The original gap (VSPCheckIsInit → GetVSPVersion) is CLOSED. A NEW gap emerged: the `_run_native_sweep()` geometry builder + solver configuration doesn't produce valid VSPAERO VLM results.
</current_state>

<completed_work>

- Plan 03-05 Task 1: Fix `_try_import_vsp()` — DONE (commit 8647010)
  - Replaced non-existent `vsp.VSPCheckIsInit()` with `vsp.GetVSPVersion()` smoke test
  - Removed dead `PTH_FILE` reference from `scripts/install_openvsp.sh`
  - All 196 tests pass
- Additional VSPAERO pipeline fixes (iterative debugging with user running python3.13):
  - Added `vsp.Update()`, `SetVSP3FileName`, `WriteVSPFile` before solver (commit f1b92e8)
  - Switched from `ComputeDegenGeom` to `ExportFile(EXPORT_VSPGEOM)` for VSPAERO v7.2.2 (commit f9a53f6)
  - Added thin/thick surface classification via user geometry sets (commit c6bcc7e)
</completed_work>

<remaining_work>

- Plan 03-05 Task 2: Human verification — BLOCKED on VSPAERO solver pipeline issues
- NEW WORK NEEDED (separate phase/plan): Fix `_run_native_sweep()` VSPAERO solver pipeline:
  1. **Thin/thick surface classification**: `ExportFile` with `thin_set=THIN_SET` not producing thin surfaces in `.vspgeom`. VSPAERO reports `NumberOfThinSurfaces_: 0`, `NumKuttaNodes: 0`. VLM requires thin panels with trailing edges for Kutta condition.
  2. **Geometry coordinate scaling**: Z coordinates in `.vspgeom` are ~1e15 (should be small values in inches). Possible unit/scaling issue in OpenVSP geometry builder.
  3. **Solver exits immediately**: VSPAERO prints "Done" without iterating because no valid thin surfaces exist to solve.
  4. **Control surface parm errors**: `FindParm::Can't Find Parm EtaStart SS_Control_*` — non-fatal but indicates geometry setup issues with auto-generated control surfaces.
  5. **`AnalysisMethod` removed**: Parameter doesn't exist in OpenVSP 3.48.2 API. Removed, but VLM vs Panel mode selection may need different approach.
</remaining_work>

<decisions_made>

- Replaced `VSPCheckIsInit()` with `GetVSPVersion()` — confirmed working in OpenVSP 3.48.2, already used in install script smoke test
- Used `EXPORT_VSPGEOM` instead of `ComputeDegenGeom` — VSPAERO v7.2.2 reads `.vspgeom` files, not old `.vspaero` DegenGeom format
- Created user geometry set (SET_FIRST_USER=3) for thin lifting surfaces — correct API approach but not producing expected results yet
- Scope decision: The VSPCheckIsInit fix IS the 03-05 gap closure. The VSPAERO solver pipeline issues are NEW scope requiring a separate phase/plan
</decisions_made>

<blockers>

- VSPAERO VLM solver cannot run because:
  - 0 thin surfaces exported (VLM needs thin panels for lift computation)
  - 0 Kutta nodes (no trailing edges → no circulation → no lift)
  - Geometry Z coordinates appear corrupted (~1e15 scale)
- This requires deeper OpenVSP API investigation — likely needs to examine how `ExportFile` handles thin_set parameter for VSPGEOM format, or whether `ComputeCFDMesh` with specific parameters is needed instead
</blockers>

<context>
The original Phase 3 verification found 2 gaps, both from the same root cause (VSPCheckIsInit). Plan 03-05 fixed the root cause successfully. However, actually running VSPAERO end-to-end revealed that `_run_native_sweep()` — while structurally correct — has never been tested against the real solver. The geometry builder sets parameters via OpenVSP API but the VSPAERO execution pipeline (file export, surface classification, solver invocation, result extraction) needs systematic debugging.

Key OpenVSP API findings from this session:
- `ExportFile(filename, thick_set, EXPORT_VSPGEOM, subsFlag, thin_set)` — correct signature
- `SET_FIRST_USER = 3` — first user-defined geometry set
- `SetSetFlag(geom_id, set_index, True)` — add geom to set
- `SetVSPAERORefWingID(wing_id)` — set reference wing for Sref/bref/cref
- VSPAEROSweep analysis inputs include `GeomSet` and `ThinGeomSet` — may need to be set explicitly
- VSPAERO v7.2.2 reads `.vspgeom` (not `.vspaero`) — binary surface mesh format

The 4 commits on this branch are all valid incremental fixes. They should NOT be reverted — each one fixed a real issue. The remaining work is additional fixes to the same `_run_native_sweep()` method.
</context>

<next_action>
1. `/clear` then `/gsd:insert-phase 3.1` to create a gap-closure phase for "VSPAERO solver pipeline debugging"
2. The new phase should investigate: (a) correct VSPGEOM export with thin surfaces, (b) geometry coordinate validation, (c) Kutta node generation, (d) end-to-end solver iteration
3. Key files to examine: `core/vsp_integration.py` lines 127-270 (`_run_native_sweep`), OpenVSP API docs for `ExportFile` thin_set behavior
4. Consider writing a minimal standalone test script that builds one wing and runs VSPAERO — isolate from the full Long-EZ model
</next_action>
